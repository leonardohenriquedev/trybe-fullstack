const randomNumberGenerator = () => {
  return parseInt(Math.random(0, 100) * 100);
};

function toUpperCase (str) {
  return str.toUpperCase();
};

const firstLetter = (str) => {
  return str[0];
};

const concat = (str, str2) =>{
  return str+str2
}